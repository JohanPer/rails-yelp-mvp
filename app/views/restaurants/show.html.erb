<h1><%= @restaurant.name %></h1>
<p>address: <%= @restaurant.address %></p>
<p>category: <%= @restaurant.category %></p>
<p>phone number: <%= @restaurant.phone_number %></p>

<%= link_to "Add a new review", new_restaurant_review_path(@restaurant), class: "btn btn-primary" %>

<%= link_to "Go back to your list", restaurants_path, class: "btn btn-primary" %>

<h3><strong>Reviews</strong></h3>
<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item">rating : <%= review.rating %> - <%= review.content %></li>
  <% end %>
</ul>
